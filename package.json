{
  "name": "payusnomind-cover-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium",
    "prestart": "chmod -R u+rx node_modules/playwright-core/.local-browsers",
    "start": "node server.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.55.0"
  }
}
